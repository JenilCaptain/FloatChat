version: "3.8"

services:
  qdrant:
    image: qdrant/qdrant:latest
    container_name: qdrant_vectordb
    ports:
      - "6333:6333" # REST API
      - "6334:6334" # gRPC API (optional)
#     volumes:
#       - ./qdrant_storage:/qdrant/storage # Persist data locally
#     environment:
#       - QDRANT__SERVICE__GRPC_PORT=6334
#     restart: unless-stopped
#     healthcheck:
#       test: ["CMD", "curl", "-f", "http://localhost:6333/health"]
#       interval: 30s
#       timeout: 10s
#       retries: 3
#       start_period: 40s

# # Optional: Add Ollama if you want to run it in Docker too
# # Uncomment the section below to include Ollama in docker-compose
# #
# #   ollama:
# #     image: ollama/ollama:latest
# #     container_name: ollama_llm
# #     ports:
# #       - "11434:11434"
# #     volumes:
# #       - ./ollama_models:/root/.ollama  # Persist models locally
# #     restart: unless-stopped
# #     # For GPU support, uncomment:
# #     # deploy:
# #     #   resources:
# #     #     reservations:
# #     #       devices:
# #     #         - driver: nvidia
# #     #           count: 1
# #     #           capabilities: [gpu]

# volumes:
#   qdrant_storage:
#     driver: local
